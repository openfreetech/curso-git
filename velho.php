git add
git commit -m "Mensagem"
git commit -a -m "Commita direto da stange area para produção"
git diff

git diff --staged

ver tudo que foi commitado
git log

para ver tudo que foi comitado e mostrar o diff
git log -p

para exibir numero de logs limitado
git log -p -1

visualizado de relatórios graficos
gitk

Mostra apenas uma linha do que foi commitado
git log --pretty=oneline
